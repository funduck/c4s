TemplateAction = {
    type : "action",
    base_char : "characteristic, that is considered when calculating effect"
};

SimpleMeleeAttack = {
    id      : "SimpleMeleeAttack",
    base_char : "ma"   
};

SimpleMeleeBlock = {
    id      : "SimpleMeleeBlock",
    base_char : "mb"
};
